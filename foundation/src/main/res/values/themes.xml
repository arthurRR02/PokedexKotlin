<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Pokédex" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryVariant">@color/light_red</item>
        <item name="colorOnPrimary">@color/gray</item>

        <item name="android:textColorHint">?attr/colorOnPrimary</item>
        <item name="android:editTextColor">?attr/colorOnPrimary</item>
        <item name="android:fontFamily">@font/pokemon_gb</item>
        <item name="android:indeterminateTint">@color/gray</item>

        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>